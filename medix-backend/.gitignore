# Build output
target/
*.class
build.log
build.txt

# IDE
.idea/

# Config & secrets — NEVER commit. Use the *.example templates instead.
src/main/resources/application.properties
src/main/resources/application-local.properties
application.properties
application-local.properties

# Uploaded user files (medical records) — runtime data, not source code
uploads/
