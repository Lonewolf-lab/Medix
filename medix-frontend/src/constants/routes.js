// Single source of truth for app paths. Import everywhere instead of string literals.
export const ROUTES = {
  landing: '/',
  login: '/login',
  register: '/register',
  dashboard: '/dashboard',
  symptoms: '/symptoms',
  records: '/records',
  medications: '/medications',
  chat: '/chat',
  profile: '/profile',
}
